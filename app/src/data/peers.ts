// Curated peers map; extend as needed. Fallback to sector-based in PeersCard.
export const curatedPeers: Record<string, string[]> = {
  // Example entries (symbol: peers)
  // 'AAPL': ['MSFT', 'GOOGL', 'AMZN'],
  // 'TSLA': ['NIO', 'RIVN', 'F'],
};
